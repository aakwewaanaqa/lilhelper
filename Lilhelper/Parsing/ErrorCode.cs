namespace Lilhelper.Parsing {
    public enum ErrorCode : uint {
        Ok               = 200,
        ExpectationFail  = 400,
        Meaningless      = 401,
        ExceptionOccured = 500,
    }
}
